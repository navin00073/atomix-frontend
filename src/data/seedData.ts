import {
  Patient,
  StaffMember,
  Medicine,
  VendingMachine,
  DeliveryRobot,
  IoTDevice,
  AuditLog,
  AlertNotification,
  MedicineTraceabilityRecord,
  PatientVisit,
  Prescription,
} from '../types';

export const INITIAL_PATIENTS: Patient[] = [
    ];
}
